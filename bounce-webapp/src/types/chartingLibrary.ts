/**
 * Re-exports from TradingView’s bundled typings in `public/charting_library/`.
 * Use this instead of deep `public/...` imports from app components.
 */
export type { ResolutionString } from "../../public/charting_library/charting_library";
