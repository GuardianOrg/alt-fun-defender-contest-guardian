{
  "name": "@launchpad/indexer",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "ponder dev",
    "start": "ponder start --hostname 0.0.0.0",
    "serve": "ponder serve",
    "codegen": "ponder codegen",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/ test/",
    "test": "vitest run --passWithNoTests",
    "test:smoke": "node scripts/smoke-test.mjs"
  },
  "dependencies": {
    "@launchpad/shared": "*",
    "hono": "^4.12.14",
    "ponder": "^0.16",
    "viem": "^2.48.2"
  },
  "devDependencies": {
    "@launchpad/config": "*",
    "@types/node": "^25.6.0",
    "typescript": "~6.0.3",
    "vitest": "^4"
  }
}
