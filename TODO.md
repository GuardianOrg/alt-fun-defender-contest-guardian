# TODO

Single source of truth for open tasks. Remove items when completed. Add items when new work is discovered.

---

Incrementally migrate remaining per-component button styles to use the shared `Button` component (`components/shared/Button.tsx`). The component is ready with `primary`, `secondary`, `danger`, and `ghost` variants. Border-radius has already been removed from all existing button CSS, but individual button elements still use local CSS classes instead of the shared component.